{
  "description": "Quality validation gate — triggers self-evaluation before task completion",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "You are a JSON classifier. Output ONLY raw JSON, no markdown fences, no explanation. Examine the stop event context and classify. $ARGUMENTS\n\nIf stop_hook_active is true, output: {\"ok\":true}\nIf last_assistant_message is conversational (greeting, question, clarification, acknowledgment), output: {\"ok\":true}\nIf last_assistant_message reports completing a task or deliverable, output: {\"ok\":false,\"reason\":\"Invoke the quality-validation skill to verify deliverables match the original request. Then assess whether changes warrant running code-quality-evaluation before committing.\"}",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
